# Tiny Hello API

Build a single Node.js file `index.js` that prints exactly `ok` and exits 0.
No framework. No long-running server. No `npm start`. `npm install` only if you
add a `package.json` — prefer zero dependencies.
